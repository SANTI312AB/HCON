{% extends 'base.html.twig' %}

{% block title %}Evolucion{% endblock %}

{% block body %}
    <h1>Evolucion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evolucion.id }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ evolucion.descripción }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_evolucion_index') }}">back to list</a>

    <a href="{{ path('app_evolucion_edit', {'id': evolucion.id}) }}">edit</a>

    {{ include('evolucion/_delete_form.html.twig') }}
{% endblock %}
