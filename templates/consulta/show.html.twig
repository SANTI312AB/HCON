{% extends 'base.html.twig' %}

{% block title %}Consulta{% endblock %}

{% block body %}
    <h1>Consulta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consultum.id }}</td>
            </tr>
            <tr>
                <th>Motivo_consulta</th>
                <td>{{ consultum.motivoConsulta }}</td>
            </tr>
            <tr>
                <th>Fecha_atencion</th>
                <td>{{ consultum.fechaAtencion ? consultum.fechaAtencion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('consulta_index') }}">
        <button class="btn btn-block btn-outline-warning btn-sm">
            <i class="fas fa-chevron-left">Regresar</i>
        </button>
    </a>

    {{ include('consulta/_delete_form.html.twig') }}
{% endblock %}
