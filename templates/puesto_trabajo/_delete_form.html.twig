<form method="post" action="{{ path('puesto_trabajo_delete', {'id': puesto_trabajo.id}) }}" onsubmit="return confirm('Está Seguro de Desea Eliminar?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ puesto_trabajo.id) }}">
    </br>
    <button class="btn btn-block btn-outline-danger btn-sm"> Eliminar</button>
</form>
