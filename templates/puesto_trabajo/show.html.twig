{% extends 'base.html.twig' %}

{% block title %}Puesto Trabajo{% endblock %}

{% block body %}
    <h1>Puesto Trabajo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ puesto_trabajo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ puesto_trabajo.nombre }}</td>
            </tr>
            <tr>
                <th>Tiempo_trabajo</th>
                <td>{{ puesto_trabajo.tiempoTrabajo }}</td>
            </tr>
            <tr>
                <th>Medidas_preventivas</th>
                <td>{{ puesto_trabajo.medidasPreventivas }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('puesto_trabajo_index') }}">Regresar</a>

    <a href="{{ path('puesto_trabajo_edit', {'id': puesto_trabajo.id}) }}">Editar</a>

    {{ include('puesto_trabajo/_delete_form.html.twig') }}
{% endblock %}
