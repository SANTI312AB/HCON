<form method="post" action="{{ path('ant_no_patologicos_delete', {'id': ant_no_patologico.id}) }}" onsubmit="return confirm('Está seguro de eliminar el item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ant_no_patologico.id) }}">
    <button class="btn btn-block btn-outline-danger btn-sm">
    <i class="fas fa-trash-alt"> 
    Eliminar
    </i>
    </button>
</form>
