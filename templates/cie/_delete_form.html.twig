<form method="post" action="{{ path('c_i_e_delete', {'id': c_i_e.id}) }}" onsubmit="return confirm('Está Seguro que Desea Eliminar?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ c_i_e.id) }}">

    <button type="button" class="btn btn-block btn-outline-danger btn-sm">
        <i class="fas fa-trash-alt"> 
            Eliminar
        </i>
    </button>
</form>
