{% extends 'base.html.twig' %}

{% block title %}CIE{% endblock %}

{% block body %}
    <h1>CIE</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ c_i_e.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ c_i_e.codigo }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ c_i_e.descripcion }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ c_i_e.tipo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('c_i_e_index') }}">Regresar</a>

    <a href="{{ path('c_i_e_edit', {'id': c_i_e.id}) }}">Editar</a>

    {{ include('c_i_e/_delete_form.html.twig') }}
{% endblock %}
