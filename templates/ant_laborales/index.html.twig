{% extends 'base.html.twig' %}

{% block title %}AntLaborales index{% endblock %}

{% block body %}
    <h1>AntLaborales index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ant_laborale in ant_laborales %}
            <tr>
                <td>{{ ant_laborale.id }}</td>
                <td>
                    <a href="{{ path('ant_laborales_show', {'id': ant_laborale.id}) }}">show</a>
                    <a href="{{ path('ant_laborales_edit', {'id': ant_laborale.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ant_laborales_new') }}">Create new</a>
{% endblock %}
