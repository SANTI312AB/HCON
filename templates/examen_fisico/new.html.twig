{% extends 'base.html.twig' %}

{% block title %}Nuevo Examen Físico{% endblock %}

{% block body %}
<div class="card-body p-5 bg-white rounded">
 {{ include('full_antecedentes/ant_base.html.twig') }}
 
{{ include('examen_fisico/_form.html.twig') }}

<br>
{% for flash_message in app.session.flashbag.get('exito') %}
    <div class="alert alert-success" role="alert">
       {{ flash_message }}
</div>
{% endfor %}

    <!-- Bordered tabs-->
    <ul id="myTab1" role="tablist" class="nav nav-tabs nav-pills with-arrow flex-column flex-sm-row text-center">
      <li class="nav-item flex-sm-fill">
        <a id="home1-tab" data-toggle="tab" href="#home1" role="tab" aria-controls="home1" aria-selected="true" class="nav-link text-uppercase font-weight-bold mr-sm-3 rounded-0 border active">Registros Consulta Actual</a>
      </li>
      <li class="nav-item flex-sm-fill">
        <a id="profile1-tab" data-toggle="tab" href="#profile1" role="tab" aria-controls="profile1" aria-selected="false" class="nav-link text-uppercase font-weight-bold mr-sm-3 rounded-0 border">REGISTROS OTRAS CONSULTAS</a>
      </li>
    </ul>


    <div id="myTab1Content" class="tab-content">
    
      <div id="home1" role="tabpanel" aria-labelledby="home-tab" class="tab-pane fade px-4 py-5 show active">
     
              <div class="table-responsive">
 <table id="example" style="width:100%" class="content-table table-lg">
        <thead>
            <tr>
            
                <th>Piel</th>
                <th>Ojos</th>
                <th>Oido</th>
                <th>Oro_farinje</th>
                <th>Nariz</th>
                <th>Cuello</th>
                <th>Torax</th>
                <th>Abdomen</th>
                <th>Columna</th>
                <th>Pelvis</th>
                <th>Extremidades</th>
                <th>Neurologico</th>
                <th>Observaciones</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody id="articles5" >
        {% for examen_fisico in examen_fisicos %}
            <tr>
                <td>{{ examen_fisico.piel ? examen_fisico.piel|json_encode : '' }}</td>
                <td>{{ examen_fisico.ojos ? examen_fisico.ojos|json_encode : '' }}</td>
                <td>{{ examen_fisico.oido ? examen_fisico.oido|json_encode : '' }}</td>
                <td>{{ examen_fisico.oro_farinje ? examen_fisico.oro_farinje|json_encode : '' }}</td>
                <td>{{ examen_fisico.nariz ? examen_fisico.nariz|json_encode : '' }}</td>
                <td>{{ examen_fisico.cuello ? examen_fisico.cuello|json_encode : '' }}</td>
                <td>{{ examen_fisico.torax1 ? examen_fisico.torax1|json_encode : ''}}</td>
                <td>{{ examen_fisico.abdomen ? examen_fisico.abdomen|json_encode : ''}}</td>
                <td>{{ examen_fisico.columna ? examen_fisico.piel|json_encode : '' }}</td>
                <td>{{ examen_fisico.pelvis ? examen_fisico.pelvis|json_encode : '' }}</td>
                <td>{{ examen_fisico.extremidades ? examen_fisico.extremidades|json_encode : '' }}</td>
                <td>{{ examen_fisico.neurologico ? examen_fisico.neurologico|json_encode : '' }}</td>
                <td>{{ examen_fisico.observaciones }}</td>
                <td>
                 <a  class="dropdown-item" href="{{ path('examen_fisico_edit', {id: examen_fisico.id , p: paciente.id , c:consulta.id}) }}" style="font-size: 0.8em">Editar Examen Fisico</a> 
                  <a href="#" class="dropdown-item delete-article" data-id="{{examen_fisico.id}}" style="font-size: 0.8em">Eliminar</a> 
                </td>
            </tr>
        {% if examen_fisicos is empty  %}
          <tr>
        <td colspan="4">No hay registros disponibles</td>
         </tr>
        {% endif %}
        {% endfor %}
        </tbody>
    </table>
</div>

     </div>


      <div id="profile1" role="tabpanel" aria-labelledby="profile-tab" class="tab-pane fade px-4 py-5">
       
     
          <div class="table-responsive">
 <table id="example2" style="width:100%" class="content-table table-lg">
        <thead>
            <tr>
                <th>Consulta</th>
                <th>Fecha_Consulta</th>
                <th>Piel</th>
                <th>Ojos</th>
                <th>Oido</th>
                <th>Oro_farinje</th>
                <th>Nariz</th>
                <th>Cuello</th>
                <th>Torax</th>
                <th>Abdomen</th>
                <th>Columna</th>
                <th>Pelvis</th>
                <th>Extremidades</th>
                <th>Neurologico</th>
                <th>Observaciones</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody id="articles55" >
        {% for examen_fisico in examen_fisicos2 %}
            <tr>
                <td>{{ examen_fisico.motivo_consulta }}</td>
                <td>{{ examen_fisico.fecha_atencion ? examen_fisico.fecha_atencion|date('Y-m-d') : '' }}</td>
                <td>{{ examen_fisico.piel ? examen_fisico.piel|json_encode : '' }}</td>
                <td>{{ examen_fisico.ojos ? examen_fisico.ojos|json_encode : '' }}</td>
                <td>{{ examen_fisico.oido ? examen_fisico.oido|json_encode : '' }}</td>
                <td>{{ examen_fisico.oro_farinje ? examen_fisico.oro_farinje|json_encode : '' }}</td>
                <td>{{ examen_fisico.nariz ? examen_fisico.nariz|json_encode : '' }}</td>
                <td>{{ examen_fisico.cuello ? examen_fisico.cuello|json_encode : '' }}</td>
                <td>{{ examen_fisico.torax1 ? examen_fisico.torax1|json_encode : ''}}</td>
                <td>{{ examen_fisico.abdomen ? examen_fisico.abdomen|json_encode : ''}}</td>
                <td>{{ examen_fisico.columna ? examen_fisico.piel|json_encode : '' }}</td>
                <td>{{ examen_fisico.pelvis ? examen_fisico.pelvis|json_encode : '' }}</td>
                <td>{{ examen_fisico.extremidades ? examen_fisico.extremidades|json_encode : '' }}</td>
                <td>{{ examen_fisico.neurologico ? examen_fisico.neurologico|json_encode : '' }}</td>
                <td>{{ examen_fisico.observaciones }}</td>
                <td>
                  <a  class="dropdown-item" href="{{ path('examen_fisico_copiar', {id: examen_fisico.id , p: paciente.id , c:consulta.id}) }}" style="font-size: 0.8em">Copiar el Examen Fisico</a> 
                </td>
               
            </tr>
          {% if examen_fisicos2 is empty  %}
             <tr>
               <td colspan="4">No hay registros disponibles</td>
             </tr>
          {% endif %}
        {% endfor %}
        </tbody>
    </table>
    </div>
      
      </div>

      </div>




  
     



</div>


{% endblock %}

{% block javascripts %}

  <script> 
       $(document).ready(function() {
        $('#example').DataTable( {
            language: {
            url: 'https://cdn.datatables.net/plug-ins/1.10.22/i18n/Spanish.json'
        },

          
         'responsive': true,
         
  } );
    } );

  
 </script>


   <script> 
       $(document).ready(function() {
        $('#example2').DataTable( {
             
            language: {
            url: 'https://cdn.datatables.net/plug-ins/1.10.22/i18n/Spanish.json'
        },
           responsive: true,
          
        
          order:[1, 'desc'],
            columnDefs: [ {
            targets: [ 0 ],
            visible: false
        } ],
            rowGroup: {
            dataSrc: [0]
        },

        
          
    
   
         
  } );
    } );

  
 </script>

   <script>

  const articles5 = document.getElementById('articles5');

   if (articles5) {
     articles5.addEventListener('click', e => {
       if (e.target.className === 'dropdown-item delete-article') {
         if (confirm('Esta seguro de eliminar este elemento?')) {
           const value = e.target.getAttribute('data-id');
             let route = "{{ path('delate_examenfisico', { id: 'fileId_' }) }}";
             route = route.replace('fileId_', value);
   
           fetch(route, {
             method: 'DELETE'
           }).then(res => window.location.reload());
         }
       }
     });
   };
  </script>

   <script>

  const articles55 = document.getElementById('articles55');

   if (articles55) {
     articles55.addEventListener('click', e => {
       if (e.target.className === 'dropdown-item delete-article') {
         if (confirm('Esta seguro de eliminar este elemento?')) {
           const value = e.target.getAttribute('data-id');
             let route = "{{ path('delate_examenfisico', { id: 'fileId_' }) }}";
             route = route.replace('fileId_', value);
   
           fetch(route, {
             method: 'DELETE'
           }).then(res => window.location.reload());
         }
       }
     });
   };
  </script>


  


 <script type="text/javascript"> 
     window.onload= function(){
       document.getElementById("op12").style="background-color: #FDF4BC;";
     }
 </script>

   {% endblock %}

