<form method="post" action="{{ path('examen_fisico_delete', {'id': examen_fisico.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ examen_fisico.id) }}">
    <button class="btn btn-primary">Eliminar</button>
</form>
